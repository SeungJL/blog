# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

이 저장소는 `blog.study-about.club` — ABOUT 서비스를 만들고 운영하는 이승주(Founder & Product Engineer)의 개발 블로그다. Astro 7 기반 정적 사이트.

Astro 관련 기본 안내는 `AGENTS.md`를 참고한다(이 파일과 별개, 심볼릭 링크 아님).

## 명령어

```bash
npm run dev       # localhost:4321
npm run build     # dist/ 생성. description 누락 등 스키마 위반 시 실패한다
npm run preview
npx astro check   # 타입 체크
```

캐시 문제로 삭제한 글이 계속 나타나면 `rm -rf node_modules/.astro .astro dist` 후 재빌드한다.

## 글 작성

`src/content/posts/`에 마크다운 파일 하나를 추가하는 것이 유일한 반복 작업이다. 나머지(RSS, sitemap, OG 이미지, 목록, canonical)는 빌드 시 자동 생성된다.

frontmatter 스키마 (`src/content.config.ts`):
```yaml
title: string          # 필수
excerpt: string        # 목록·검색·OG에 노출됨. 사실상 필수로 취급한다
publishDate: datetime  # 필수 (pubDate/pubDatetime 아님)
updateDate: datetime   # 선택
category: string       # 선택
tags: string[]         # 선택
draft: boolean         # 선택 — true면 빌드 결과에서 제외
image: string          # 선택 — 커버 이미지 URL
---
```

**글 파일은 `src/data/post/`에 둔다** (`src/content/posts/` 아님).
URL은 `/posts/<파일명>` 으로 생성된다 (`src/config.yaml`의 `permalink` 설정).

### 작성 워크플로

1. 소재를 정하면 Claude가 사실·숫자·구조·코드 인용 위주로 초안을 작성해 `draft: true`로 커밋한다.
2. 사용자가 본문에 `<!-- ME: ... -->` 형태로 느낀 점·맥락을 다듬지 않고 삽입한다.
3. Claude가 그 조각을 문맥에 녹여 최종안을 만들고 `<!-- ME: -->` 마커를 제거한다.
4. `draft: false`로 바꾸고 push한다.

**Claude가 지어내면 안 되는 것: 저자가 무엇을 느꼈는지, 왜 이 작업을 이 시점에 했는지, 팀에 어떻게 설명했는지.** 이런 맥락은 반드시 `<!-- ME: -->`로 받은 내용에서만 가져온다. 사용자가 "적당히 써줘"라고 해도 실제 경험에 해당하는 자리는 비워두고 이유를 설명한다 — 면접에서 답할 수 없는 문장이 글에 있으면 안 된다.

`<!-- ME: -->`는 **꼭 필요한 곳에만 남긴다.** 다섯 개씩 뿌리면 사용자가 채우는 부담만 커지고 대부분은 없어도 되는 자리다. 글에 장면이 하나도 없을 때 **한 곳만** 요청하는 게 낫다.

## 톤 원칙 — 가장 중요하다

이 블로그는 **이미 많은 것을 만들고 운영해본 개발자이자 창업가**가 쓴다. 취업 준비와 포트폴리오를 겸하므로, **글쓴이가 부족해 보이게 만드는 표현을 절대 기본값으로 쓰지 않는다.**

| 쓰지 않는다 | 대신 이렇게 |
|---|---|
| "내실은 그 자리에 있었다" | "실무에서는 자신이 있었다. 다만 그 경험이 정리되지 않은 채 쌓여 있었다" |
| "탐구를 안 한 지 오래됐다" | "만드는 데 시간을 다 썼고, 정리할 시간은 따로 두지 못했다" |
| "그 사이 세상이 바뀌었다" (수동) | "그 시기는 크게 바뀐 때였고, 나도 그 안에서 직접 만들며 통과했다" (능동) |
| "정체됐다" / "뒤처졌다" | "정리가 필요하다" / "깊이 파볼 때가 됐다" |
| "부족한 부분을 메우겠다" | "가진 것을 무기로 만들겠다" |

**결핍을 채우는 서사가 아니라 축적을 정리하는 서사로 쓴다.** 이 차이가 글 전체의 인상을 가른다.

세상의 변화를 수동적으로 겪은 것처럼 쓰지 않는다. 자기 상태를 점검하는 대목은 **자신감을 먼저 분명히 쓰고 그다음에 관찰을 붙인다.** 순서가 바뀌면 반성문이 된다.

### 방어하지 않는다

- 아무도 하지 않은 주장에 반박하지 않는다. ("~라는 결론으로 가는 건 너무 쉽다" 같은 문장은 없는 상대와 싸우는 것이다)
- 자기를 증명하려는 항변을 넣지 않는다. ("기사로 읽은 게 아니라 직접 겪었다" 류)
- 담담하게 사실을 서술하면 방어보다 강하다.

## 문체

- **평서체** (`~했다`, `~이다`). 존댓말 쓰지 않는다.
- **독자는 실무 개발자.** `peer dependency`, `lockfile`, `SSR` 같은 용어를 설명 없이 쓴다.
- **길이는 소재에 맞춘다.** 채우려고 늘리지 않는다. 초안이 길면 중복부터 잘라낸다.
- 1인칭은 기술 글에서는 최소화한다. 다만 **회고·발간사처럼 본인 경험이 소재인 글은 1인칭이 자연스럽다.** 소재에 따라 판단한다.

### 구조 — 기술 글

```
1. 상황과 제약   — 첫 문단에 숫자로. "1,086파일 / 11만 줄 / 테스트 0개"
2. 처음 가정한 것 — 그리고 왜 틀렸는지
3. 실제로 측정한 것 — 표로. 이게 글의 자산이다
4. 판단과 근거   — 왜 A를 택하고 B를 버렸는가
5. 틀린 것 / 못 한 것 — 생략하지 않는다. 여기서 신뢰가 생긴다
6. 결과          — 전후 비교, 남은 것
```

### 구조 — 회고·경험 글

서사가 곧 논증이다. 주장을 먼저 내세우지 말고 사건으로 도달한다.

```
1. 사건에서 시작   — 구체적 장면 하나로 연다
2. 거기서 무엇이 자라났는지
3. 상황이 바뀐 지점
4. 지금 어떻게 보는지 — 자신감을 먼저, 관찰을 그다음에
5. 그래서 무엇을 하는지 — 1절과 연결해 닫는다
```

**글 전체에 최소 하나의 장면이 있어야 한다.** 요약과 주장만으로 채우면 잘 정리된 진술서처럼 읽힌다. 장면 하나가 글의 질감을 바꾼다.

**마지막 문단은 앞쪽 절로 회수한다.** 첫머리에서 세운 것을 끝에서 다시 만나면 글이 닫힌다.

### 덕목을 단어로 쓰지 않는다

"문제 해결력", "주도성", "책임감", "열정" 같은 단어를 직접 쓰지 않는다. 사건으로만 보여준다. 이런 단어가 들어가는 순간 자기소개서가 되고 읽는 사람은 그 차이를 안다.

자기규정("나는 ~한 개발자다")은 **글 전체에서 한 번만** 쓴다. 반복하면 주장이 되고, 한 번 쓰고 나머지를 사건으로 증명하면 근거가 된다.

### 절대 쓰지 않는 표현 (AI 티 나는 패턴)

- "안녕하세요, 오늘은 ~에 대해 알아보겠습니다" 류의 서론
- "도움이 되셨길 바랍니다" 류의 맺음 인사
- 글 서두에 목차를 나열
- 근거 없는 강조어 ("놀랍게도", "충격적이게도")
- 불릿으로만 채운 문단 — 설명은 문장으로, 불릿은 순수 나열에만
- "결론부터 말씀드리면"의 남발
- **극단적 단정** ("~하지 않으면 재사용되지 않는다" 류). 단호해 보이지만 반박당하기 쉽고 실제로 과장이다.

### 사실 규칙

- 숫자는 실측치만 쓴다. 어림값이면 "약"을 붙인다.
- **서비스 지표(가입자 수, API 개수, 매출)는 나열하지 않는다.** 자랑으로 읽히고 서사의 힘을 깎는다. "네 명으로 시작해 지금은 만 명 단위" 정도의 서술로 충분하다. 단 **판단 근거가 되는 기술 측정치(영향 파일 수 등)는 예외** — 그건 자랑이 아니라 논증의 재료다.
- 코드를 인용할 때는 실제 파일 경로를 명시한다.
- 확인하지 않은 것은 "확인하지 않았다"고 명시한다. 추측을 단정으로 쓰지 않는다.
- **AI와 함께한 작업을 "AI가 틀렸는데 내가 잡았다"로 각색하지 않는다.** 더 극적이지만 사실이 아니고, 정직한 프레이밍("무엇을 확인해야 하는지 알고 있었다")이 논지에도 더 잘 맞는다.

### 초안을 줄일 때 자르는 순서

1. 같은 말을 두 번 하는 절 — 병합한다
2. 방어적 문장 (위 "방어하지 않는다" 참조)
3. 앞 문장과 겹치는 마무리 문장
4. 도입부 요약 ("앞으로 쓸 것은 크게 세 가지다" → 바로 항목으로)
5. 나열의 중복 ("결제를 붙이고, 인증을 붙이고" → "결제와 인증을 붙이고")

**본문 근거가 되는 나열은 마지막에 자른다.** 그게 사라지면 주장만 남는다.

## 마스킹 필수 (About 저장소 내용을 인용할 때)

- AWS 계정 ID, ECR 전체 경로 → `<account-id>`로 대체
- FontAwesome / Chromatic 토큰 값 — 절대 노출하지 않는다
- EC2 서버 경로, Secrets Manager 시크릿 이름
- `scripts/deploy.sh` 전문 — 롤백 취약점이 그대로 드러난다

## 아키텍처 메모

- **테마는 [AstroWind](https://github.com/onwidget/astrowind)를 쓴다.** Astro 7.3.1 + Tailwind 4로 우리 환경과 버전이 일치한다. 테마 파일을 함부로 고치지 않고 설정으로 해결되는지 먼저 확인한다.
- **설정은 `src/config.yaml` 한 곳에서 한다.** 사이트명·도메인·SEO 기본값·블로그 경로·테마 모드가 모두 여기 있다.
- **메뉴는 `src/navigation.ts`.** 헤더/푸터 링크를 여기서 관리한다.
- 원본 AstroWind는 마케팅 사이트 템플릿이라 랜딩·가격표·서비스 페이지가 딸려 있었다. **블로그만 남기려고 `src/pages/landing`, `src/pages/homes`, `pricing`, `services`, `contact`, `terms`, `privacy`, `public/decapcms`를 제거했다.** 테마를 업데이트할 때 다시 딸려 들어오지 않게 주의한다.
- **홈(`src/pages/index.astro`)이 곧 글 목록이다.** `fetchPosts()`를 직접 부른다. `getStaticPathsBlogList`는 `/blog` 경로 전용이라 홈에서 쓰면 `page`가 undefined가 되어 빌드가 깨진다.
- `/blog` 라우트는 살아 있지만 `robots.index: false`로 색인에서 뺐다. 홈과 중복이기 때문이다.
- 글이 쌓여 페이지네이션이 필요해지면 홈에 `paginate`를 붙이거나 `/blog`로 넘긴다.
- 서비스(`study-about.club`)와 별도 배포 파이프라인이다. 글 하나 고치자고 About의 CodeBuild를 태우지 않기 위한 의도적 분리다.
